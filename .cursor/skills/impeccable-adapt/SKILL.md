---
name: impeccable-adapt
description: >-
  Adapt desktop UI to mobile/tablet (or vice versa) using Impeccable's adapt
  workflow. Use when refactoring layouts for new viewports, adding mobile card
  views, or rethinking navigation for touch — not just scaling pixels.
---

# Impeccable Adapt

Read and follow **`.agents/skills/impeccable/reference/adapt.md`** before adapting UI.

## Addbell context

- Employee portal: `EpMobileBlock` / `EpDesktopBlock`, `ep*` tokens in `lib/employee-portal-ui.ts`, rules in `.cursor/rules/employee-portal-mobile.mdc`
- Dashboard: `DbMobileBlock` / `DbDesktopBlock`, `db*` tokens in `lib/dashboard-ui.ts`, rules in `.cursor/rules/admin-dashboard-mobile.mdc`
- Project skill: `.cursor/skills/addbell-mobile-ux/SKILL.md`

## Related skills (already in repo)

- `.agents/skills/impeccable/` — full Impeccable toolkit
- `.agents/skills/web-design-guidelines/` — Vercel web design guidelines
- `.agents/skills/mobile-responsiveness/` — mobile-first patterns
- `.agents/skills/responsive-testing/` — multi-viewport verification workflow
